{% extends 'base.html.twig' %}

{% block title %}Hello ProfilPersonelController!{% endblock %}

{% block body %}
 <section id="profil">
        <div class="bloc-profil">
            <div class="image-perso">
                <div class="blog-pserso">
                   <img src="{{asset('image/profile.png')}}" alt="image">
                </div>    
            </div>

            <div class="datauser">
                <div class="datauser2">
                    <div class="infouser">
                        <p>Nom : {{app.user.Nom}}</p>
                        <div class="tre"></div>
                        <P>Prenom : {{app.user.Prenom}}</p>
                        <div class="tre"></div>
                        <p>Mail : {{app.user.email}}</p>
                        <div class="tre"></div>
                        <p>Contacte : {{app.user.Contacte}} <p>
                            <div class="tre"></div>
                        <p>Adresse : {{app.user.Adresse}} <p>
                        <div class="tre"></div>
                        <p>CreatAt : {{app.user.CreatAt|date("m/d/Y")}} <p>
                    </div>

                   
                    <div class="bloc-btn">
                        <div class="btn-div">
                             <a target="__blank" href="{{path('app_create_profil', {'id': app.user.id})}}"><button class="btn-2 btn-1">Create Profil</button></a>
                                <a target="__blank" href="{{ path('app_update_profil',{'id': app.user.id})}}"><button class="btn-2 btn-1">Modifier</button></a>
                        </div>
                        <div class="btn-div2">
                                {{ include('profil_personel/_delete.html.twig') }}
                                {% if is_granted('ROLE_ADMIN') %}
                                   <form>
                                        <button class="bt btn-2" formaction="{{ path('app_admin')}}">Admin</button>
                                    </form>
                                   
                                {% endif %}   
                        </div>
                      
                  
                    </div>
                </div>    
               
                 
            </div>
           
        </div>
    </section>
{% endblock %}
